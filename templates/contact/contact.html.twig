{% extends 'base.html.twig' %}

{% block title %}Contact Form{% endblock %}

{% block body %}
    <div>
        {{ form_start(our_form) }}
        {{ form_widget(our_form) }}
        <div class="form-group row">
            <div class="col-sm-2"></div>
            <div class="col-sm-10">
                <input type="submit" name="contact_info" class="btn btn-success">
            </div>
        </div>

        {{ form_end(our_form) }}
    </div>
{% endblock %}
